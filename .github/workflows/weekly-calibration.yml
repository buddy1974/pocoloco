name: weekly-calibration

on:
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder — not yet implemented
        run: echo "Routine weekly-calibration is not yet implemented. Extend this workflow when ready."
