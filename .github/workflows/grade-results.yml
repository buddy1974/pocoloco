name: Grade Results

on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:

jobs:
  grade:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Call grade endpoint
        run: |
          STATUS=$(curl -s -o /tmp/out.json -w "%{http_code}" \
            -X POST \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
            "${{ secrets.SITE_URL }}/api/grade")
          cat /tmp/out.json
          [ "$STATUS" = "200" ] || { echo "HTTP $STATUS"; exit 1; }
