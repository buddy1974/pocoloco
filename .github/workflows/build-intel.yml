name: build-intel

on:
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder — not yet implemented
        run: echo "Routine build-intel is not yet implemented. Extend this workflow when ready."
